# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as passwords, private keys, and other secrets. 
# However, these should be included in the repository if you intend to use version control to manage these files.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so are not needed in the repository
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using a negated pattern
!example_override.tf

# Include tfplan files to ignore terraform plan output
*.tfplan

# Exclude .terraformrc and terraform.rc, which are config files for Terraform and may contain sensitive data
.terraformrc
terraform.rc

